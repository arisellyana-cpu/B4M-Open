function rule = Rule()

rule.ID = [];

rule.TargetClass = [];

rule.Conditions = [];

rule.NumConditions = 0;

rule.Quality = 0;

rule.Coverage = 0;

rule.Fitness = 0;

rule.DescriptionLength = 0;

rule.IsPruned = false;

end